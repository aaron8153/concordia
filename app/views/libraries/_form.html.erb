<%= form_for(@lib) do |f| %>
  <% if @lib.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lib.errors.count, "error") %> prohibited this library from being saved:</h2>

      <ul>
      <% @lib.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
